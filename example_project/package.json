{
  "private": true,
  "name": "example_project",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test:local": "vitest run",
    "test:local:headed": "VITEST_BROWSER_HEADLESS=0 vitest",
    "test:browserstack": "broyster run --config vitest.browserstack.config.ts",
    "test:browserstack:cloudflare": "broyster run --config vitest.browserstack.config.ts --transport cloudflare"
  },
  "devDependencies": {
    "@fpjs-incubator/broyster": "workspace:*",
    "@vitest/browser": "^4.1.5",
    "@vitest/browser-playwright": "^4.1.5",
    "playwright": "^1.60.0",
    "vitest": "^4.1.5"
  }
}
